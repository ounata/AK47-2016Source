define([ppts.config.modules.infra,
        ppts.config.dataServiceConfig.extraGiftDataService
],
    function (schedule) {
        schedule.registerController('presentEditController', [

            function () {
                var vm = this;

            }]);
    });