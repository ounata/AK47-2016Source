define([ppts.config.modules.infra,
        ppts.config.dataServiceConfig.extraGiftDataService
],
    function (schedule) {
        schedule.registerController('presentAddController', [

            function () {
                var vm = this;

            }]);
    });