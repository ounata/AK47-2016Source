define([ppts.config.modules.infra,
        ppts.config.dataServiceConfig.extraGiftDataService
],
    function (schedule) {
        schedule.registerController('presentDetialController', [

            function () {
                var vm = this;

            }]);
    });